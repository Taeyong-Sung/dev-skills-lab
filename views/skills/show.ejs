<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Skill Details</h1>
    <h2>Skill #<%= skill._id %></h2>
    <form
      action="/skills/<%= skill._id %>?_method=DELETE"
      class="delete-form"
      method="POST"
    >
      <button type="submit">X</button>
    </form>
    <h3><%= skill.text %></h3>
    <a href="/skills/<%= skill._id %>/edit">Edit</a>
    <p>Acquired: <%= skill.acquired ? 'Done' : 'Not yet.' %></p>
    <a href="/skills">Go back</a>
  </body>
</html>
